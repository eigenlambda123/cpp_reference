#include <iostream>

int main() {
    int x; // define a variable (object with a name) of type int
    x = 42; // store a value in the object x

    std::cout << "The value of x is: " << x << std::endl; // access and print the value

    return 0;
}
